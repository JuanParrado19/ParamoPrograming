import React from 'react';
import { Container } from 'reactstrap';

const Home = () => (
    <Container fluid className="d-flex align-items-center justify-content-center" style={{position: 'relative', height: '100vh'}}>
        <h1 className="display-1 text-center">ParamoPrograming</h1>
    </Container>
);

export default Home;